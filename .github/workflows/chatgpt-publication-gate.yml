name: chatgpt-publication-gate

on:
  push:
    branches: [chatgpt-publication-grade-gate]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build exact production image
        run: docker build -t aulaai-publication-gate .
