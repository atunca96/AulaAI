name: chatgpt-authoring-fix

on:
  push:
    branches: [chatgpt-terra-bilingual-assessment-fix]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build exact production image
        run: docker build -t aulaai-authoring-fix .
