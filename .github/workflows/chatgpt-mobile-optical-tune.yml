name: chatgpt-mobile-optical-tune
on:
  push:
    branches: [chatgpt-mobile-optical-tune]
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt-mobile-optical-tune
      - name: Tune mobile optical spacing
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('public/css/styles.css'); s=p.read_text()
          old='body:has(> .screen.active:not(#login-screen)) { padding-bottom: 14vh; }'
          new='body:has(> .screen.active:not(#login-screen)) { padding-top: 6vh; padding-bottom: 8vh; }'
          if s.count(old)!=1: raise SystemExit(f'expected optical rule once, found {s.count(old)}')
          p.write_text(s.replace(old,new,1))
          p=Path('public/index.html'); s=p.read_text()
          old='/css/styles.css?v=20260917_optical01'; new='/css/styles.css?v=20260917_optical02'
          if s.count(old)!=1: raise SystemExit(f'expected css version once, found {s.count(old)}')
          p.write_text(s.replace(old,new,1))
          PY
      - name: Verify and commit
        run: |
          git diff --check
          git diff -- public/css/styles.css public/index.html
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/css/styles.css public/index.html
          git commit -m "fix: tune mobile short-page optical spacing"
          git push origin HEAD:chatgpt-mobile-optical-tune
